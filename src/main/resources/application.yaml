spring:
  datasource:
    username: postgres
    password: developer
    url: jdbc:postgresql://localhost:5432/product
  jpa:
    hibernate:
      ddl-auto: create-drop

  application:
    name: product-service

server:
  port: 8081

#axon:
#  axonserver:
#    servers: localhost
